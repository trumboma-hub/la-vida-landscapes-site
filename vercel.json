{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'no root install — studio installs itself in buildCommand'",
  "buildCommand": "cd studio && npm ci && npm run build && cd .. && rm -rf admin && mv studio/dist admin",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/static/(.*)", "destination": "/admin/static/$1" },
    { "source": "/admin/((?!.*\\.).*)", "destination": "/admin/index.html" }
  ]
}
