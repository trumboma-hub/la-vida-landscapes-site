{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'no root install — studio installs itself in buildCommand'",
  "buildCommand": "npm install -g npm@11.6.2 && cd studio && npm ci && npm run build && cd .. && rm -rf admin && mv studio/dist admin && rm -rf studio",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/static/(.*)", "destination": "/admin/static/$1" },
    { "source": "/admin/((?!.*\\.).*)", "destination": "/admin/index.html" }
  ]
}
